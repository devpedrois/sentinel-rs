# sentinel

Lightweight Rust CLI for monitoring CPU, memory, disk, and network metrics.

## Em desenvolvimento

The project foundation is in progress and the monitoring engine will arrive in the next PRs.

## Build

```bash
cargo build
```
